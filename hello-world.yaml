apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: helloremote
spec:
  steps:
    - name: echo
      image: alpine
      script: |
        #!/bin/sh
        echo "Hello Remote World"        
